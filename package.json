{
  "name": "babeilove",
  "version": "1.0.0",
  "description": "Private couple's diary and media timeline",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "install:backend": "cd backend && npm install",
    "hash-password": "cd backend && npm run hash-password"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
